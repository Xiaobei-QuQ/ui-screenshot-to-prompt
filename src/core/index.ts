// 导出主要功能和类型
export { processImage, launchInterface, reactProcessImage } from './main'
export { VISION_ANALYSIS_PROMPT, SYSTEM_PROMPT, setSplittingMode, getSplittingMode, setPromptChoice, getPromptChoice, MIN_COMPONENT_WIDTH, MIN_COMPONENT_HEIGHT } from './config'

// 导出所有类型定义
export * from './types'
